import java.sql.SQLOutput;
import java.util.*;

public class Main {
    public static char capToSmall(char ch) {
        int conv = ch - 'A';
        return (char)('a' + conv);
    }
    public static void main(String[] args) {
//        String str = "Hello I am a Student";
//        String ans = "";
//
//        for(int i=0; i<str.length(); i++){
//            char ch = str.charAt(i);
//            if(ch >= 'A' && ch <= 'Z') ch = capToSmall(ch);
//
//            if(ch == 'a' || ch == 'e' || ch == 'i' || ch == 'o' || ch == 'u') {
//                if(i==0) ans += str.charAt(i);
//                else if(str.charAt(i-1) == ' ') ans += str.charAt(i);
//            }
//        }
//
//        System.out.println(ans);

    }
}